# Increase memory for the Gradle Daemon to 4GB to prevent "GC Thrashing" errors
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g

# Enable AndroidX (required for Jetpack Compose)
android.useAndroidX=true